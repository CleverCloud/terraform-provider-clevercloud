---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "clevercloud_oauth_consumer Resource - terraform-provider-clevercloud"
description: |-
  Manage OAuth consumers for Clever Cloud organizations.
  OAuth consumers allow third-party applications to access Clever Cloud APIs on behalf of users. This resource manages the consumer credentials and permissions.
  Example Usage
  
  provider "clevercloud" {
    organisation = "orga_xxxx-xxxx-xxxx-xxxx-xxxx"
  }
  
  resource "clevercloud_oauth_consumer" "my_app" {
    name        = "My Application"
    description = "OAuth consumer for my application"
    base_url    = "https://api.myapp.com"
    logo_url    = "https://myapp.com/logo.png"
    website_url = "https://myapp.com"
  
    rights = [
      "access_organisations",
      "manage_organisations_applications",
    ]
  }
  
  # Access the generated credentials
  output "oauth_key" {
    value = clevercloud_oauth_consumer.my_app.id
  }
  
  output "oauth_secret" {
    value     = clevercloud_oauth_consumer.my_app.secret
    sensitive = true
  }
  
  Available Rights
  The following rights can be granted to an OAuth consumer:
  access_organisations - View organization informationaccess_organisations_bills - View organization billsaccess_organisations_consumption_statistics - View consumption statisticsaccess_organisations_credit_count - View credit countaccess_personal_information - View personal informationmanage_organisations - Manage organization settingsmanage_organisations_applications - Manage applicationsmanage_organisations_members - Manage organization membersmanage_organisations_services - Manage servicesmanage_personal_information - Manage personal informationmanage_ssh_keys - Manage SSH keys
---

# clevercloud_oauth_consumer (Resource)

Manage OAuth consumers for Clever Cloud organizations.

OAuth consumers allow third-party applications to access Clever Cloud APIs on behalf of users. This resource manages the consumer credentials and permissions.

## Example Usage

```terraform
provider "clevercloud" {
  organisation = "orga_xxxx-xxxx-xxxx-xxxx-xxxx"
}

resource "clevercloud_oauth_consumer" "my_app" {
  name        = "My Application"
  description = "OAuth consumer for my application"
  base_url    = "https://api.myapp.com"
  logo_url    = "https://myapp.com/logo.png"
  website_url = "https://myapp.com"

  rights = [
    "access_organisations",
    "manage_organisations_applications",
  ]
}

# Access the generated credentials
output "oauth_key" {
  value = clevercloud_oauth_consumer.my_app.id
}

output "oauth_secret" {
  value     = clevercloud_oauth_consumer.my_app.secret
  sensitive = true
}
```

## Available Rights

The following rights can be granted to an OAuth consumer:

- `access_organisations` - View organization information
- `access_organisations_bills` - View organization bills
- `access_organisations_consumption_statistics` - View consumption statistics
- `access_organisations_credit_count` - View credit count
- `access_personal_information` - View personal information
- `manage_organisations` - Manage organization settings
- `manage_organisations_applications` - Manage applications
- `manage_organisations_members` - Manage organization members
- `manage_organisations_services` - Manage services
- `manage_personal_information` - Manage personal information
- `manage_ssh_keys` - Manage SSH keys



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `base_url` (String) Base URL of the OAuth consumer application
- `logo_url` (String) URL of the consumer's logo/picture
- `name` (String) Name of the OAuth consumer
- `rights` (Set of String) Set of OAuth rights/permissions. Available rights: access_organisations, access_organisations_bills, access_organisations_consumption_statistics, access_organisations_credit_count, access_personal_information, manage_organisations, manage_organisations_applications, manage_organisations_members, manage_organisations_services, manage_personal_information, manage_ssh_keys
- `website_url` (String) Website URL of the OAuth consumer

### Optional

- `description` (String) Description of the OAuth consumer

### Read-Only

- `id` (String) OAuth consumer unique identifier (also used as the OAuth client ID/key)
- `secret` (String, Sensitive) OAuth consumer secret (client secret)
